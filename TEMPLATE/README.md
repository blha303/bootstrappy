%(name)s
=======

%(desc)s

Usage
-----

todo

Installation
------------

Via `pip`:

    pip3 install %(name)s

Alternatively:

 * Clone the repository, `cd %(name)s`
 * Run `python3 setup.py install` or `pip3 install -e`
